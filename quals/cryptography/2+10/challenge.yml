name: "2+10"
# This is used to group challenges in the CTFd UI.
# choose one: binary exploitation, reverse engineering, web exploitation, cryptography, forensics, miscellaneous.
category: cryptography
description: |- 
    mendaki gunung (semeru) dan menggelinding ke bawah (seperti seekor landak)

    NB: this chall has been tested on the remote server with 20 simultaneous connections with 100% winrate. please try to solve this challenge in your local machine before trying it on the remote server.

    Author: **yellow**

attribution: yellow

value: 1000
type: dynamic
extra:
    initial: 1000
    decay: 31
    minimum: 100

connection_info: nc 20.195.43.216 8101

flags:
    # A static case sensitive flag
    - ARKAV{side_channel_lewat_pc_probset_apa_lewat_waktu???hehehe}


# Provide paths to files from the same directory that this file is in
files:
    - dist/chall.zip

# If provided, the field can take one of two values: hidden, visible.
state: visible

# Specifies what version of the challenge specification was used.
version: "0.1"